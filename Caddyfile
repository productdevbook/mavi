# What answers on the address, and how it gets a certificate. The clean
# release currently exposes the canonical API; the panel will be mounted here
# after it is regenerated from the same contract.
{$MAVI_DOMAIN} {
	reverse_proxy api:8080
}
