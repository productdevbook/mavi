[package]
name = "mavi-api"
version = "0.0.0"
edition = "2024"
publish = false
description = "What an endpoint is, said completely enough that the description generated from it is worth writing a client against."

[dependencies]
mavi-core = { path = "../mavi-core" }

serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
