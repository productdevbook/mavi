[package]
name = "mavi-settings"
version = "0.0.0"
edition = "2024"
publish = false
description = "What this site is: its name, what it writes in, and which of those is its own."

[dependencies]
mavi-api = { path = "../mavi-api" }
mavi-core = { path = "../mavi-core" }
mavi-people = { path = "../mavi-people" }

serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
