[package]
name = "mavi-work"
version = "0.0.0"
edition = "2024"
publish = false
description = "Work that happens after the answer: one table, claimed with a lease."

[dependencies]
mavi-core = { path = "../mavi-core" }
mavi-db = { path = "../mavi-db" }

chrono = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
sqlx = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt", "rt-multi-thread"] }

[lints]
workspace = true
