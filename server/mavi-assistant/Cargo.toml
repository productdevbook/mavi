[package]
name = "mavi-assistant"
version = "0.0.0"
edition = "2024"
publish = false
description = "What an assistant can do here: the API it already has, said in the protocol assistants speak."

[dependencies]
mavi-api = { path = "../mavi-api" }

serde_json = { workspace = true }

[lints]
workspace = true
