[package]
name = "mavi-db"
version = "0.0.0"
edition = "2024"
publish = false
description = "Where the rows are, and the one place a listing's order is turned into SQL."

[dependencies]
mavi-core = { path = "../mavi-core" }

sqlx = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt"] }
uuid = { workspace = true }

[lints]
workspace = true
