[package]
name = "mavi-edge"
version = "0.0.0"
edition = "2024"
publish = false
description = "What a visitor sees: which file answers an address, and where a page went."

[dependencies]
uuid = { workspace = true }

[lints]
workspace = true
