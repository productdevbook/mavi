-- A site's wording lives in `letters`, one row per letter and language, and a
-- campaign carries its own body. This was written before either and nothing
-- has ever read or written it.

drop table mail_templates;
