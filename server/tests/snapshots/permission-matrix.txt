principal                          capability access  owner        answer
---------------------------------- ---------- ------- ------------ ------
holds the grant                    content    view    none         allow
holds only their own, theirs       content    view    them         allow
holds only their own, another's    content    view    somebody     deny
holds only their own, none named   content    view    none         deny
holds the grant                    content    write   none         allow
holds only their own, theirs       content    write   them         allow
holds only their own, another's    content    write   somebody     deny
holds only their own, none named   content    write   none         deny
holds the grant                    content    delete  none         allow
holds only their own, theirs       content    delete  them         allow
holds only their own, another's    content    delete  somebody     deny
holds only their own, none named   content    delete  none         deny
holds the grant                    media      view    none         allow
holds only their own, theirs       media      view    them         allow
holds only their own, another's    media      view    somebody     deny
holds only their own, none named   media      view    none         deny
holds the grant                    media      write   none         allow
holds only their own, theirs       media      write   them         allow
holds only their own, another's    media      write   somebody     deny
holds only their own, none named   media      write   none         deny
holds the grant                    media      delete  none         allow
holds only their own, theirs       media      delete  them         allow
holds only their own, another's    media      delete  somebody     deny
holds only their own, none named   media      delete  none         deny
holds the grant                    taxonomy   view    none         allow
holds only their own, theirs       taxonomy   view    them         allow
holds only their own, another's    taxonomy   view    somebody     deny
holds only their own, none named   taxonomy   view    none         deny
holds the grant                    taxonomy   write   none         allow
holds only their own, theirs       taxonomy   write   them         allow
holds only their own, another's    taxonomy   write   somebody     deny
holds only their own, none named   taxonomy   write   none         deny
holds the grant                    taxonomy   delete  none         allow
holds only their own, theirs       taxonomy   delete  them         allow
holds only their own, another's    taxonomy   delete  somebody     deny
holds only their own, none named   taxonomy   delete  none         deny
holds the grant                    forms      view    none         allow
holds only their own, theirs       forms      view    them         allow
holds only their own, another's    forms      view    somebody     deny
holds only their own, none named   forms      view    none         deny
holds the grant                    forms      write   none         allow
holds only their own, theirs       forms      write   them         allow
holds only their own, another's    forms      write   somebody     deny
holds only their own, none named   forms      write   none         deny
holds the grant                    forms      delete  none         allow
holds only their own, theirs       forms      delete  them         allow
holds only their own, another's    forms      delete  somebody     deny
holds only their own, none named   forms      delete  none         deny
holds the grant                    mail       view    none         allow
holds only their own, theirs       mail       view    them         allow
holds only their own, another's    mail       view    somebody     deny
holds only their own, none named   mail       view    none         deny
holds the grant                    mail       write   none         allow
holds only their own, theirs       mail       write   them         allow
holds only their own, another's    mail       write   somebody     deny
holds only their own, none named   mail       write   none         deny
holds the grant                    mail       delete  none         allow
holds only their own, theirs       mail       delete  them         allow
holds only their own, another's    mail       delete  somebody     deny
holds only their own, none named   mail       delete  none         deny
holds the grant                    flows      view    none         allow
holds only their own, theirs       flows      view    them         allow
holds only their own, another's    flows      view    somebody     deny
holds only their own, none named   flows      view    none         deny
holds the grant                    flows      write   none         allow
holds only their own, theirs       flows      write   them         allow
holds only their own, another's    flows      write   somebody     deny
holds only their own, none named   flows      write   none         deny
holds the grant                    flows      delete  none         allow
holds only their own, theirs       flows      delete  them         allow
holds only their own, another's    flows      delete  somebody     deny
holds only their own, none named   flows      delete  none         deny
holds the grant                    courses    view    none         allow
holds only their own, theirs       courses    view    them         allow
holds only their own, another's    courses    view    somebody     deny
holds only their own, none named   courses    view    none         deny
holds the grant                    courses    write   none         allow
holds only their own, theirs       courses    write   them         allow
holds only their own, another's    courses    write   somebody     deny
holds only their own, none named   courses    write   none         deny
holds the grant                    courses    delete  none         allow
holds only their own, theirs       courses    delete  them         allow
holds only their own, another's    courses    delete  somebody     deny
holds only their own, none named   courses    delete  none         deny
holds the grant                    shop       view    none         allow
holds only their own, theirs       shop       view    them         allow
holds only their own, another's    shop       view    somebody     deny
holds only their own, none named   shop       view    none         deny
holds the grant                    shop       write   none         allow
holds only their own, theirs       shop       write   them         allow
holds only their own, another's    shop       write   somebody     deny
holds only their own, none named   shop       write   none         deny
holds the grant                    shop       delete  none         allow
holds only their own, theirs       shop       delete  them         allow
holds only their own, another's    shop       delete  somebody     deny
holds only their own, none named   shop       delete  none         deny
holds the grant                    people     view    none         allow
holds only their own, theirs       people     view    them         allow
holds only their own, another's    people     view    somebody     deny
holds only their own, none named   people     view    none         deny
holds the grant                    people     write   none         allow
holds only their own, theirs       people     write   them         allow
holds only their own, another's    people     write   somebody     deny
holds only their own, none named   people     write   none         deny
holds the grant                    people     delete  none         allow
holds only their own, theirs       people     delete  them         allow
holds only their own, another's    people     delete  somebody     deny
holds only their own, none named   people     delete  none         deny
holds the grant                    settings   view    none         allow
holds only their own, theirs       settings   view    them         allow
holds only their own, another's    settings   view    somebody     deny
holds only their own, none named   settings   view    none         deny
holds the grant                    settings   write   none         allow
holds only their own, theirs       settings   write   them         allow
holds only their own, another's    settings   write   somebody     deny
holds only their own, none named   settings   write   none         deny
holds the grant                    settings   delete  none         allow
holds only their own, theirs       settings   delete  them         allow
holds only their own, another's    settings   delete  somebody     deny
holds only their own, none named   settings   delete  none         deny
holds the grant                    publish    view    none         allow
holds only their own, theirs       publish    view    them         allow
holds only their own, another's    publish    view    somebody     deny
holds only their own, none named   publish    view    none         deny
holds the grant                    publish    write   none         allow
holds only their own, theirs       publish    write   them         allow
holds only their own, another's    publish    write   somebody     deny
holds only their own, none named   publish    write   none         deny
holds the grant                    publish    delete  none         allow
holds only their own, theirs       publish    delete  them         allow
holds only their own, another's    publish    delete  somebody     deny
holds only their own, none named   publish    delete  none         deny
holds the grant                    design     view    none         allow
holds only their own, theirs       design     view    them         allow
holds only their own, another's    design     view    somebody     deny
holds only their own, none named   design     view    none         deny
holds the grant                    design     write   none         allow
holds only their own, theirs       design     write   them         allow
holds only their own, another's    design     write   somebody     deny
holds only their own, none named   design     write   none         deny
holds the grant                    design     delete  none         allow
holds only their own, theirs       design     delete  them         allow
holds only their own, another's    design     delete  somebody     deny
holds only their own, none named   design     delete  none         deny
holds the grant                    boards     view    none         allow
holds only their own, theirs       boards     view    them         allow
holds only their own, another's    boards     view    somebody     deny
holds only their own, none named   boards     view    none         deny
holds the grant                    boards     write   none         allow
holds only their own, theirs       boards     write   them         allow
holds only their own, another's    boards     write   somebody     deny
holds only their own, none named   boards     write   none         deny
holds the grant                    boards     delete  none         allow
holds only their own, theirs       boards     delete  them         allow
holds only their own, another's    boards     delete  somebody     deny
holds only their own, none named   boards     delete  none         deny
holds the grant                    audit      view    none         allow
holds only their own, theirs       audit      view    them         allow
holds only their own, another's    audit      view    somebody     deny
holds only their own, none named   audit      view    none         deny
holds the grant                    audit      write   none         allow
holds only their own, theirs       audit      write   them         allow
holds only their own, another's    audit      write   somebody     deny
holds only their own, none named   audit      write   none         deny
holds the grant                    audit      delete  none         allow
holds only their own, theirs       audit      delete  them         allow
holds only their own, another's    audit      delete  somebody     deny
holds only their own, none named   audit      delete  none         deny
